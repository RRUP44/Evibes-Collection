info:
  name: Tracking Por EventoId
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}Eventos/ObtenerEventoTracking/:id"
  params:
    - name: id
      value: "359"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
