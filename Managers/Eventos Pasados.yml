info:
  name: Eventos Pasados
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}Eventos/ObtenerListaPasadosVMPorIdManager/:id"
  params:
    - name: id
      value: "169"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
