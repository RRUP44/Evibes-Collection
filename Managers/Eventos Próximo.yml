info:
  name: Eventos Próximo
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}Eventos/ObtenerListaProximosVMPorIdManager/:id"
  params:
    - name: id
      value: "169"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
